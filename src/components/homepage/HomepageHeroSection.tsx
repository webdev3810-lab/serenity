/**
 * Re-exports the full HomepageHeroGallery component from its canonical location.
 * Provides a single import path for all homepage section components.
 */
export { default } from "@/src/components/HomepageHeroGallery";
export type { HomepageHeroImage } from "@/src/components/HomepageHeroGallery";
